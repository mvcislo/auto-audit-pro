---
description: Stage all changes, commit with a message, and push to origin/main
---

// turbo-all

1. Stage all changes
```
git add -A
```

2. Commit with a descriptive message (use the context of the current changes)
```
git commit -m "<describe changes here>"
```

3. Push to origin
```
git push
```
